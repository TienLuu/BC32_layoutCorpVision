/*
    INIT SLICK
*/

// Carousel
$(document).ready(function () {
   $(".carousel__content").slick({
      autoplay: true,
      fade: true,
      loop: true,
      pauseOnFocus: false,
      pauseOnHover: false,
      speed: 2000,
   });
});
